# Minimal foundry.toml for receipton
# This is a receipt generator, not a Solidity project, but cast requires
# a config to find the project root.
[profile.default]
src = "scripts"
out = "out"
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
